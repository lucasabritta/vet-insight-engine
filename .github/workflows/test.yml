name: Tests

on:
  push:
    branches: [master]
  pull_request:

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  test-backend:
    name: Tests Backend
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: "3.11"

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"

      - name: Install system packages and Python dependencies
        run: |
          sudo apt-get update && sudo apt-get install -y tesseract-ocr
          pip install -r backend/requirements.txt

      - name: Run unit tests
        run: |
          pytest backend/tests/unit --cov=backend/app --cov-report=term-missing

      - name: Run integration tests
        run: |
          pytest backend/tests/integration --cov=backend/app --cov-report=term-missing --cov-append

  test-frontend:
    name: Tests Frontend
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Node
        uses: actions/setup-node@v4
        with:
          node-version: "20"

      - name: Install dependencies
        working-directory: frontend
        run: npm install

      - name: Run tests
        working-directory: frontend
        run: npm run test:coverage
